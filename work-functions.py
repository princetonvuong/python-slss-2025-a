# Functions
# Author: Princeton
# 10 October

# Average
def average(num_one: float, num_two: float, num_three: float):
    """Return the average of three given numbers"""
    result = (num_one + num_two + num_three) / 3
    return result


print(average(88, 81, 83))
